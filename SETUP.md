# 🚀 إعداد متغيرات البيئة في Netlify

## خطوات الإعداد

### 1️⃣ إنشاء بوت Telegram

1. افتح [@BotFather](https://t.me/BotFather) في تيليجرام
2. أرسل الأمر `/newbot`
3. اتبع التعليمات لإنشاء البوت
4. احفظ الـ **Bot Token** الذي سيعطيك إياه

### 2️⃣ الحصول على Chat ID

1. أضف البوت إلى المجموعة الخاصة بك
2. اجعل البوت **Admin** في المجموعة
3. أرسل أي رسالة في المجموعة
4. افتح هذا الرابط في المتصفح (استبدل TOKEN):
   ```
   https://api.telegram.org/botYOUR_BOT_TOKEN/getUpdates
   ```
5. ابحث عن `"chat":{"id":-xxxxxxxxxx` - هذا هو الـ Chat ID

### 3️⃣ إضافة المتغيرات في Netlify

1. افتح إعدادات الموقع في Netlify.
2. اذهب إلى **Environment variables**.
3. أضف المتغيرين التاليين لكل بيئات النشر التي ستختبرها أو تستخدمها:

| المتغير | القيمة | مثال |
|---------|--------|------|
| `MDAD_BOT_TOKEN` | التوكن الكامل الذي أعطاك إياه BotFather | قيمة خاصة — لا تضعها في الموقع أو ترسلها لأحد |
| `MDAD_CHAT_ID` | معرّف مجموعة تيليجرام المستهدفة | غالبًا يبدأ معرّف المجموعة بـ `-100` |

4. احفظ التغييرات وأعد نشر الموقع (Redeploy). لا تضع هاتين القيمتين في ملفات الموقع أو في `js/app.js`.

للوصول إلى معرّف المجموعة: أضف البوت إلى المجموعة، امنحه صلاحية إرسال الرسائل، ثم أرسل رسالة اختبار في المجموعة واستخرج قيمة `chat.id` من تحديثات البوت.

---

## ✅ التحقق من عمل النظام

بعد إضافة المتغيرات وإعادة النشر:

1. افتح الموقع
2. املأ نموذج الطلب
3. اضغط على "إرسال الطلب"
4. تصل نسخة الطلب إلى المجموعة المحددة في `MDAD_CHAT_ID`.
5. يفتح الموقع رسالة جاهزة إلى حساب `Rv9_h`؛ يجب على مقدم الطلب الضغط على **إرسال** بنفسه. هذه الرسالة الشخصية لا يرسلها البوت تلقائيًا.
6. إذا لم تصل نسخة المجموعة، يعرض الموقع خطأ ويبقي البيانات في النموذج لإعادة المحاولة.

---

## 🔒 الأمان

- ✅ التوكن ومعرّف المجموعة يُقرآن في وظيفة Netlify على الخادم فقط
- ✅ لا يمكن للمستخدمين رؤية البيانات الحساسة
- ✅ كل طلب له **كود مطابقة** من 8 أحرف وأرقام
- ✅ نص المستخدم يُعالج بأمان قبل إرساله بتنسيق HTML إلى Telegram
- ✅ تفعيل حماية المحتوى يمنع إعادة توجيه الرسالة من Telegram

---

## 📝 كيفية التحقق من الطلبات

عندما يصلك طلب من المستخدم:

1. قارن **كود المطابقة** في رسالة المستخدم
2. مع **كود المطابقة** في رسالة البوت
3. إذا تطابقا → الطلب صحيح ولم يُعدّل ✅
4. إذا اختلفا → الطلب قد تم تعديله ⚠️

---

## 🎨 الخط المستخدم

تم استخدام خط **Daken** للعناوين من:
```
https://sytra.site/fonts/api/v1?search=daken
```

---

## 🛠️ الملفات المهمة

- `netlify/functions/send-to-bot.js` - Serverless function للإرسال الآمن
- `js/app.js` - منطق التطبيق مع نظام التحقق
- `netlify.toml` - إعدادات Netlify

---

## 📞 الدعم

إذا واجهت أي مشكلة:
- تأكد من أن البوت **Admin** في المجموعة
- تحقق من صحة التوكن والشات آيدي
- راجع الـ Console في المتصفح للأخطاء
